export declare abstract class IdleExpiry {
    protected idValue: any;
    constructor();
    id(value?: any): any;
    abstract last(value?: Date): Date;
    now(): Date;
    isExpired(): boolean;
}
